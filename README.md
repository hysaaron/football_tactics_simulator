# FIELDLAB · NFL 战术实验室

本地优先、零依赖的 NFL 攻防战术沙盘。借鉴战术选择卡片的操作形式，以圆形和叉号呈现 11 对 11 演化。所有界面为中文，战术保留英文名称方便对照学习。

## 本地运行

需要 Node.js 20 或更新版本，无需安装依赖。

```sh
npm run dev
```

打开 http://127.0.0.1:4173 。支持子路径部署，全部静态资源在 `dist/`，不要通过 `file://` 打开 ES 模块。

## 使用

1. 选择进攻和防守球队。32 队均可选择，球队载入示意默认战术，可以自由切换全部概念。
2. 在攻防标签下选择战术，设置开球位置、首攻距离、接球目标、出手时机和随机种子。
3. 开始、暂停、逐帧、拖动时间轴或改变播放速度；切换路线、防守区域或镜像。
4. 运行 100 次，以连续种子比较平均推进、首攻达成率和传球完成率。改变配置会清空过期统计。

## 模型与数据边界

- 6 个进攻概念、4 个防守概念；不是官方 Madden 或真实赛季球队战术库。所有球队共用同一能力分布，球队不影响能力评分。
- 固定步长 0.05 秒，最长 8 秒。时间上限为截断结果，不等于真实比赛终止。
- 防守包含区域目标、盯人、简化冲传阻挡与追击。跑球包含预设跑卫路线，未实现完整区域阻挡决策。
- 传球概率由防守分离距离、目标纵深和随机种子决定。无真实 NFL 数据拟合；批量统计不能用于比赛预测。
- 未实现犯规、掉球、真实球员、完整比赛管理或官方规则裁决。
- 球队目录来源：[NFL 官方球队列表](https://www.nfl.com/teams/)。默认概念映射为本项目教学预设，不声称反映当前球队倾向。

## 修改战术

编辑 `dist/data.js`。路线由 `[横向码数, 相对开球线的纵向码数]` 航点组成；横向范围 0–53.3，纵向正数为推进方向。`engine.js` 为可独立测试的模拟核心；`app.js` 管理状态、SVG 渲染和交互；`style.css` 定义响应式布局。

```sh
npm test
npm run check
```

## 后续部署到 GitHub Pages

当前仅在本地运行，没有创建仓库或发布网站。效果确定后可上传至 GitHub；保留的手动工作流 `.github/workflows/pages.yml` 不会因普通推送而发布。在仓库 Settings → Pages 中选择 GitHub Actions，随后手动运行 Deploy Pages。

参考：[GitHub Pages 自定义工作流](https://docs.github.com/en/pages/getting-started-with-github-pages/using-custom-workflows-with-github-pages)。

可选的 WebMCP `simulate_matchup` 接口仅在支持 `document.modelContext` 的浏览器注册；普通浏览器无需此能力。
